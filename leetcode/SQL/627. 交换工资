--更新语句update
--case when <判断表达式> then <表达式>
--       when <判断表达式> then <表达式>
--       ...
--       else <表达式>
--       end

--# Write your MySQL query statement below
update salary
set sex=(case sex when 'm' then 'f'
                  else 'm'
                  end);